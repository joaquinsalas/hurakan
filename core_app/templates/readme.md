# `core_app/templates`

This directory does not contain `.py` or `.m` source scripts.

## Overview
- It stores the HTML templates used by the Flask frontend, including login, maintenance, status, and error views.
